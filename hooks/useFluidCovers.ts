import { UseImagesQuery_allFile_nodes } from './__generated__/UseImagesQuery';

// Note: This hook is not currently used in the Next.js implementation
// Stubbed out to prevent build errors - returns empty array
export const useFluidCovers = (): UseImagesQuery_allFile_nodes[] => {
  return [];
};
